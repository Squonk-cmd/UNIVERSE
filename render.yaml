services:
  - type: web
    name: ielts-evaluator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
```

**Also fix the deprecation warning** while you're at it — update `requirements.txt`:
```
fastapi
uvicorn
pydantic
google-genai
python-multipart
python-dotenv
